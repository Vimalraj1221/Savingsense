import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.manojit94.app',
  appName: 'Savingsense',
  webDir: 'www/browser'
};

export default config;
